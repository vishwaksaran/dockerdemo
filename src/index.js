import React from 'react';
import { render } from 'react-dom';

const App = () => (
    <div>
        <h1>Legacy Migration Dashboard</h1>
        <p>React is running successfully!</p>
        {/* Placeholder for migrated components */}
    </div>
);

render(<App />, document.getElementById('root'));
